# 陪读社区 · 体验入口说明

面向**高考陪读家长**的社区型小程序（Flutter Web）。本文档说明如何进入体验、五大模块怎么用、以及开发态（demo）下的行为约定。

---

## 1. 如何进入与登录

- 体验入口为运行中的 Web 预览（本环境由反代在 **8081** 端口提供，点预览按钮即可打开）。
- 登录方式为**手机号 + 短信验证码**，开发态下验证码直接返回，无需真实短信：
  1. 输入任意手机号（如 `13800000000`）→ 点获取验证码；
  2. 后端会在登录短信接口的返回里直接给出 `dev_code`（开发态验证码）；
  3. 填入该 `dev_code` 即可登录，自动创建账号并进入首页。
- **首次登录会自动灌入样例分析数据**（多次模拟考 + 三档知识点诊断），「分析」页因此对任何新账号都开箱即有内容。

> 提示：每次用**新手机号**登录都会得到一个干净、数据一致的演示账号；分析列表条数与整体统计的「分析次数」始终相等（见第 3 节）。

---

## 2. 五大底部 Tab 导航

底部导航共 5 个入口，**首页 = 日历**，结构如下：

| 顺序 | Tab | 说明 |
|------|-----|------|
| 1 | **日历**（首页） | 高考倒计时、重要节点、家长可手动添加备考事件 / 记录模考成绩 |
| 2 | **分析** | 试卷分析：顶部整体统计 + 整体分析，下方「过往分析」列表（点列表项看详情） |
| 3 | **社区** | 虎扑风格家长社区：发帖支持图片、评论支持图片、顶部圈子 Tab 样式优化 |
| 4 | **消息** | 通知（API 驱动，未读红点，点击标记已读）+ 私信（开发态模拟） |
| 5 | **我的** | 个人资料、家长认证入口、我的邀请码 |

---

## 3. 试卷分析（重点）

满足需求「过往分析内容列表及详情 + 顶部数据统计与整体分析」。

- **顶部整体统计卡片（整体分析）**
  - 平均得分率、较首次提升（百分点）、覆盖知识点数；
  - 一句话 `summaryText` 整体结论；
  - 三档分布进度条：**待改进 / 需关注 / 继续保持**，按各档知识点数量占比展示。
- **过往分析列表**
  - 每次模拟考一条：科目标签、考试日期、三档知识点小圆点、得分率；
  - 点击任意一条进入**分析详情页**，展示该次考试的知识点三档诊断（待改进 / 需关注 / 继续保持 分组卡片）。
- **数据一致性保证**
  - 「列表条数」与「整体统计的分析次数」来自**同一份样例数据源**：每次考试都同时落库 `Exam` + `Paper` + 知识点三档结果，因此列表条数 = 统计次数，二者恒等，不会出现「统计说 7 次、列表只 1 条」的错位。

> 数据均由后端 `seedSampleData` 幂等预置；历史账号若曾灌入旧样例，可用新手机号登录获得最新一致数据。

---

## 4. 社区（重点）

满足需求「发帖支持图片、列表与详情特殊样式、评论支持图片、发布页 UI 优化、顶部 Tab 样式优化」。

- **发帖支持图片**：发布页可一次选择多张图片（九宫格预览 + 可删除），先逐张上传到后端，再随帖子提交；列表卡片与帖子详情页对图片做**大图 Banner / 九宫格**特殊样式展示，多图显示「+N」。
- **评论支持图片**：帖子详情页评论框支持选图并随评论上传，评论区可显示图片。
- **顶部圈子 Tab 样式优化**：白底圆角胶囊、选中态品牌红、带阴影容器，视觉更清晰。
- 其余保留虎扑风格：点赞、关注、分享报告到社区等。

---

## 5. 消息 Tab（新增）

满足需求「新增底部消息 Tab 用于社区通知或 IM 聊天」。

- **通知**：由后端 `/api/notifications` 驱动，含评论 / 点赞 / 关注 / 系统四类样例；未读项显示红点，点击后调用 `/api/notifications/read` 标记为已读。
- **私信**：开发态以模拟会话列表 + 本地发送呈现（IM 后端为占位实现，消息仅本地可见）。

---

## 6. 家长认证（解锁上传 / 发帖）

满足「家长通过认证解锁上传与社区发帖」。关键修复点：**JWT 中的角色可能与数据库中最新角色不一致**——`VerifiedMiddleware` 现在会**回源数据库重新拉取当前角色**，避免「已认证通过但仍 403」的问题。

- 路径：「我的」→ 家长认证 → 选择**邀请码认证**；
- 体验用邀请码：**`PAIDU8`**（已预置，长期有效、可多次使用）；
- 使用后会把当前账号角色升级为 `parent`，即可在「分析」页上传试卷、在「社区」发帖。

> 之前存在的「认证通过却仍无法发帖」问题，根因是 JWT 角色为旧值；本次已通过中间件回源 DB 修复，可端到端验证：用新手机号登录（未认证）→ 使用 `PAIDU8` → 同一令牌即可发帖/上传成功。

---

## 7. 开发态约定（重要）

- **图片上传**：开发态直接保存到后端 `uploads/` 目录，并经反代以 `/api/uploads/*` 静态访问，**不接对象存储**。前端用 `image_picker` 的 `XFile.readAsBytes()` 读取，Web 端同样可用。
- **IM 私信**：为模拟数据，非真实即时通讯。
- **短信验证码**：`/api/auth/send-sms` 直接返回 `dev_code`。
- **样例数据**：分析、通知等均为幂等预置，刷新或重登不会重复灌入。

---

## 8. 技术栈与本地启动

- 前端：Flutter Web（CanvasKit，已关闭 Service Worker 缓存、启用本地 canvaskit 以保证预览环境稳定）。
- 后端：Go (Gin + GORM) + PostgreSQL + FastAPI(OCR)。
- 启动：由 `run/start_stack.sh` 单入口拉起（释放端口 → PostgreSQL(docker) → OCR(8000) → 后端(8080) → 反代(8081)），由 supervisord 托管、可自动重启。
- 关键后端端点：
  - `GET /api/papers/overview` 整体分析统计
  - `GET /api/papers` 我的过往分析列表
  - `GET /api/papers/:id/report` 单次分析详情
  - `POST /api/upload` 图片上传（开发态）
  - `GET /api/notifications`、`POST /api/notifications/read` 消息通知
  - `POST /api/invites/use` 使用邀请码（家长认证）
