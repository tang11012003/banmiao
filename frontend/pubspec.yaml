name: peidu_community
description: 陪读社区 App - 面向高考家长的移动社区（工具+社区）
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.0.0
  provider: ^6.0.5
  fl_chart: ^0.66.0
  image_picker: ^1.0.0
  shared_preferences: ^2.2.0
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
